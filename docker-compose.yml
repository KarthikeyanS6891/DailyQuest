services:
  postgres:
    image: postgres:16-alpine
    container_name: dailyquest-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dailyquest
      POSTGRES_PASSWORD: dailyquest
      POSTGRES_DB: dailyquest
    volumes:
      - dailyquest_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dailyquest -d dailyquest"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:latest
    container_name: dailyquest-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  dailyquest_pg:
